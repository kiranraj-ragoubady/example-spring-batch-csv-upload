# example-spring-batch-csv-upload
To upload a csv file to a database table without using any hardcoded model classes. File, table and field mapping configurations are set in a JSON file under resources.
